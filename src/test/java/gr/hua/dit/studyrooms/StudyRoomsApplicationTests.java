package gr.hua.dit.studyrooms;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

// Indicates that the class is a Spring Boot test and will start the application context
@SpringBootTest
class StudyRoomsApplicationTests {

    // Test method to check if the Spring application context loads without errors
    @Test
    void contextLoads() {
        // If the application context fails to load, this test will fail
    }

}
